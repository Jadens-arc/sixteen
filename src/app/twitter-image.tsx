// X/Twitter reads its own tag. Sharing the Open Graph card keeps one image to
// maintain instead of two that drift apart.
export { default, alt, size, contentType, dynamic } from "./opengraph-image";
